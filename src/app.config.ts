import { AppConfig } from 'remax/wechat'

const config: AppConfig = {
  pages: ['pages/index/index'],
  window: {
    navigationBarTitleText: '滴答',
    navigationBarTextStyle: 'black',
    navigationBarBackgroundColor: '#fff'
  }
}

export default config
