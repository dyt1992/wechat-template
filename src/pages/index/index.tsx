import * as React from 'react'
import { View } from 'remax/wechat'

export default () => {
  return (
    <View
      style={{
        width: '100vw',
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
      }}>
      Hollow, World
    </View>
  )
}
